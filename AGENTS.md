# 项目协作约定

- 默认中文交流。此仓库公开，代码和公开文档不得包含本机绝对路径。
- README 以简体中文为主（README.md），英文版为 README.en-us.md；两版逐行对应、总行数一致，冲突时以中文版为准，改动需同步。
- 两版顶部跳转块固定为两行且完全相同，位置在 H1 与一句话简介之后：`> **[📖 English](README.en-us.md)**`、`> **[📖 简体中文(大陆)](README.md)**`。
- 开始工作时读取存在的 AGENTS.local.md；本机配置不提交。
- 从项目根目录使用 uv sync --locked 和 uv run python 运行工具。
- 歌词、标题、歌手和署名统一使用霞鹜文楷，通过 render.py --path <字体文件> 注册；不硬编码路径，不静默回退字体。字体文件不复制到仓库。
- 公开命令使用占位路径；本机完整命令仅记录于 AGENTS.local.md。
- 保留 Write、关键词语义配色、片尾微笑弧线，以及顶部真实频谱/波形与底部进度条；底部植物线稿不再恢复。片尾标题与弧线在最后 `OUTRO_FADE`（3 s）内淡出，结束于最后一帧。
- 时间轴是时间来源，删除动画不能提前后续歌词。行首与染色词的 Write 提前量（LEAD_IN、KEYWORD_LEAD）只是动画预滚，不得据此改动时间轴数据；染色词的收笔时刻由 KEYWORD_TAIL 固定，必须与原时间轴一致。估算时间不标记为人工校对。
- 渲染中间产物通过 --work 指定目录，遵守本机临时目录约定；完成后仅清理本次临时文件，正式成片保留在 output。
- 修改后检查字体加载、关键词排版、时长、帧率和完整解码，并实际检查视频抽帧；未通过的检查须如实报告。
- 保护 music 原始素材，署名遵循 ASSET_NOTICE.md。
